name: Test Microsoft Graph

on:
  workflow_dispatch:

jobs:
  test-login:
    runs-on: ubuntu-latest

    steps:
      - name: Visa att workflow fungerar
        run: |
          echo "Workflow fungerar"
          echo "Client ID finns: ${{ secrets.AZURE_CLIENT_ID != '' }}"
          echo "Tenant ID finns: ${{ secrets.AZURE_TENANT_ID != '' }}"
          echo "Client Secret finns: ${{ secrets.AZURE_CLIENT_SECRET != '' }}"
